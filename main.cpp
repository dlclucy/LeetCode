#include <iostream>
using namespace std;
class HouseRobber {
public:
    int rob(vector<int> &num){
        if(num.size()<=1)
            return num.empty()?0:num[0];

    }

    int main() {
        int p;
        cin >> p;
        std::cout << "Hello, World!" << std::endl;
        return 0;
    }
};